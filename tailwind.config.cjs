module.exports = {
    theme: {
        fontFamily: {
            // Define as many custom font families as needed
            outfit: ['Outfit', 'sans-serif'],       // All weights (100-900)
            'work-sans': ['Work Sans', 'sans-serif'] // Only weight 300
        }
    }
};